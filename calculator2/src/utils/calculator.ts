
export const somar = (number, number2) => {
  const result = number + number2;

  return result;
}

export const subtrair = (number, number2) => {
  const result = number - number2;

  return result;
}

export const dividir = (number, number2) => {
  const result = number / number2;

  return result;
}

export const multiplicar = (number, number2) => {
  const result = number * number2;

  return result;
}